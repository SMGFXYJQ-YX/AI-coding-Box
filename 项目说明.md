# 项目复盘说明（做了什么 · 遇到什么问题 · 怎么解决）

> 本文档覆盖简历中的**全部项目**，按「做了什么 → 遇到什么问题 → 怎么解决」的复盘视角组织，便于面试时顺着讲实战经历。
> 所有内容均对应真实代码 / 测试记录，无虚构功能、无夸大数据。

---

## 项目总览

| # | 项目名称 | 我的角色 | 核心技术 | 一句话 |
|---|---|---|---|---|
| 1 | AI 对话智能体应用实践 | 设计 + 自动化实现 | Python + requests + pytest | 对接 Qwen 大模型 API，落地 55 条接口自动化用例 |
| 2 | 网页小游戏 Demo（独立开发） | 独立前端开发 | HTML / CSS / JavaScript | 贪吃蛇、扫雷、化学反卡牌三款可运行网页游戏 |
| 3 | AI 代码助手 | 独立全栈开发 | Python + Flask + Qwen API | 网页版 AI 编程工具，支持代码生成 / 解释审查 / 测试用例生成 |

---

## 一、AI 对话智能体应用实践

### 做了什么
- 选择一个大模型对话接口（通义千问 `Qwen/Qwen2.5-7B-Instruct`，通过硅基流动 SiliconFlow 云端 API 调用）作为测试对象；
- 用 Python + `requests` 调接口、`pytest` 落地自动化，基于参数化设计了 **55 条**测试用例，覆盖功能、边界、异常、AI 特有四类场景；
- 测试策略分两条线：自动化负责结构断言（非空 / 超时 / 拒答 / JSON / 代码 / 长度约束），手工负责质量断言（事实 / 逻辑 / 幻觉 / 错误前提）；
- 执行中发现并记录 **6 个真实缺陷**（D-002~D-007），产出用例表、缺陷表、测试报告，全部开源到 `github.com/SMGFXYJQ-YX/ai-chatbot-testing`。

### 遇到什么问题
1. **本地环境太慢**：最初想用 Ollama 在本地跑模型，但模型下载预计要 5 小时，根本没法快速开测。
2. **脚本初轮出现误报**：第一轮跑出 51 通过、4 失败。其中有 2 条（TC-031、TC-042）其实是模型已经正确拒绝了不当请求（话术是"我必须强调…""作为一个人工智能…"），但我的断言关键词没匹配到，造成误判；另有 1 条是网络超时异常（非模型缺陷），1 条是真实缺陷（未遵守「100 字以内」，实际输出 171 字）。
3. **模型输出存在退化与幻觉**：实测中出现乱码、叠字、断字、重复字符；还有事实性错误——比如称月球既是"地球第三颗卫星"又说是"唯一天然卫星"，自相矛盾。

### 怎么解决
- **环境**：放弃本地部署，改用 SiliconFlow 的免费云端 API，无需下载即调即用，测试立刻能跑起来；
- **误报**：扩充拒绝关键词库，让脚本能识别更多拒答话术，把 2 条误报转为可判定通过；超时那条标记为需重跑确认，不计入模型缺陷；只有真实的长度越界缺陷保留；
- **退化 / 幻觉**：这类属于模型本身能力问题，我将其记录为缺陷并标注「暂不修复」，同时在报告里给出风险建议（关键场景多次采样、加强 prompt 约束、引入事实核查）。我能讲清每条缺陷的现象、复现步骤和定位依据。

### 收获
理解了 **AI 应用测试和传统软件测试的本质区别**：AI 输出是非确定的，不能全靠脚本断言，必须「自动化做结构 + 手工做质量」两条线结合。

---

## 二、网页小游戏 Demo（独立开发）

### 做了什么
- 独立用原生 HTML / CSS / JavaScript 开发了三款网页小游戏：贪吃蛇（方向控制 + 计分 + 碰撞检测）、扫雷（连锁展开 + 插旗 + 胜负判定 + 计时）、化学反卡牌（闯关 / 学习双模式 + 拖拽出牌 + 道具系统）；
- 对三款游戏做了一轮系统手工功能测试，共执行 **57 条功能用例**（贪吃蛇 10 + 扫雷 10 + 化学卡片 37 + 11 条数据校验全部通过），发现 **13 个真实缺陷**（D-001~D-013）；
- 配套完整测试文档：测试计划、用例表（Excel 分游戏 sheet）、缺陷表、README，代码开源到 `github.com/SMGFXYJQ-YX/game`。

### 遇到什么问题
1. **一开始对游戏理解错了**：化学卡片我最初以为是「记忆翻牌」类，按错误理解草拟了一批用例，方向完全偏了。
2. **缺陷判定出现失误**：数据校验环节初判有 2 项失败，但复核实测值（如 128 / 3）其实和预期一致，是我自己判定错了。
3. **缺陷严重级不敢乱定**：很多缺陷的现象能看清，但严重程度拿不准，怕瞎编等级被追问穿帮。

### 怎么解决
- **理解错玩法**：主动向项目方确认真实玩法是「化学反卡牌」，用真实的 37 条用例替换掉错误草拟的内容，并补了 11 条数据校验（卡池 48、反应 128、零反应卡 BaSO₄ 等），保证用例贴合真实游戏；
- **判定失误**：坚持「存疑就回去复核实测值」的原则，把这 2 项移出缺陷计数，最终缺陷定稿为 **13 条**，确保数字真实、不编；
- **严重级**：如实标注「待评估 / 待确认」，没有编造严重级；面试时能说清每条缺陷的现象和复现步骤即可。

### 收获
锻炼了用例设计（等价类、边界值、场景法）和缺陷定位、复现能力；养成了「数据不编、判定存疑就复核实测值」的严谨习惯。

---

## 三、AI 代码助手

### 做了什么
- 用 Python + Flask 搭建了一个网页版 AI 编程工具：前端输入需求或粘贴代码，后端调用 Qwen 大模型返回结果；
- 在模型之上实现了自己的**业务逻辑层**——同一模型通过不同提示词承担三种角色：**代码生成**（自然语言 → 代码）、**代码解释 / 审查**（代码 → 功能说明 + 问题 + 优化建议）、**测试用例生成**（代码 → 覆盖正常 / 边界 / 异常的单元测试用例）；
- 支持 Python / JavaScript / Java / C++ / HTML-CSS / SQL，结果带一键复制，Procfile 已备好可部署到 Render。

### 遇到什么问题
1. **如果只是套壳调 API，竞争力很弱**：纯前端套个壳调模型，谁都会做，在 AI 岗简历里没有说服力。
2. **密钥不能写死、也不能进仓库**：API 密钥一旦硬编码进代码或提交到 GitHub，会泄露；但又要让本地运行和云端部署都能方便拿到密钥。
3. **部署到云平台时端口必须可变**：本地固定端口，但 Render 等平台通过环境变量 `PORT` 指定端口，写死会部署失败。

### 怎么解决
- **业务逻辑层**：不只用一套提示词，而是写了 `build_messages()` 按 `mode` 字段切换 system prompt，让同一个模型在不同模式下干不同的事。并把「测试用例生成」和我之前的测试项目背景串起来——「我会写测试，所以工具能生成测试」，形成差异化；
- **密钥安全**：实现双通道——优先读环境变量，读不到时回退到本地 `key.local` 文件；`key.local` 被 `.gitignore` 排除，绝不会进仓库；
- **端口可变**：代码读取环境变量 `PORT`，本地默认 5001（避开常见端口占用），云端由平台注入，Procfile 的启动命令与之匹配。

### 收获
做出了一件比「套壳」更有含量的 AI Coding 作品：核心是「在调用大模型 API 之上实现了自己的逻辑层」，同时工程规范（密钥管理、跨域、异常处理、部署配置）都做到位。

---

## 合集级诚实声明

1. **数据真实**：所有用例数、缺陷数、技术栈均对应真实项目文件，未虚构。
2. **视角说明**：项目一在简历中表述为「AI 对话智能体应用实践」（应用 / 调试视角），其底层是真实的 pytest 接口自动化测试实践；项目二在简历中表述为「独立开发 Demo」，其底层包含真实的系统化功能测试。文档与简历表述一致。
3. **运行形态**：项目一、二为本地 / 开源代码形态；项目三为本地运行版，公开网址需部署后获得（Procfile 已备）。
4. **模型局限**：使用的 Qwen2.5-7B-Instruct 为轻量级开源模型，复杂任务效果有限，但足以演示完整产品闭环。
